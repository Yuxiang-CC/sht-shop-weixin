<navigator wx:for="{{noticeList}}" wx:key="id" url="/pages/notice/show?id={{item.noticeId}}">
  <view class='cashlogs'>
    <view class='profile'>
      <view class='typeStr'>{{item.noticeTitle}}</view>
      <view class='dateAdd'>{{ item.createTime }}</view>
    </view>
    <view class='amount'> 查看 > </view>
  </view>
</navigator>